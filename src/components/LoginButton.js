import {Button} from "@tremor/react";
import {UserIcon} from "@heroicons/react/outline";

// eslint-disable-next-line import/no-anonymous-default-export
export default () => (
    <Button variant="light" icon={UserIcon}>
        Login
    </Button>
);